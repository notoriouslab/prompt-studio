# OS / editor
.DS_Store
*.swp
*~

# Claude Code per-machine state
.claude/

# Personal reference (not for upstream)
demo.md
提示詞範本/

# Node (in case anyone adds tooling later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local secrets (never commit)
.env
.env.*
*.secrets.json
.gstack/

# Private distributions (kept local, not for upstream)
docs/notebooklm-distribution/
docs/gemini-gem-distribution/
